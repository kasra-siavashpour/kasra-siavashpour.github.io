# macOS System Files
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Editor and IDE Files
*.swp
*.swo
*~
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Backup files
*.bak
*.backup
*.tmp

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Node modules (if you add any build tools later)
node_modules/
package-lock.json
yarn.lock

# Environment variables
.env
.env.local
.env.*.local

# Build outputs (if you add build processes)
dist/
build/
*.min.js
*.min.css

# Compressed files
*.zip
*.tar.gz
*.rar

# Personal notes (optional - uncomment if needed)
# notes/
# TODO.md
# NOTES.md

**Key Features of this `.gitignore`:**

1. **macOS-Specific:** Covers all common macOS system files (`.DS_Store`, thumbnails, Spotlight indexes, etc.)
2. **Editor Files:** Excludes VSCode, IntelliJ IDEA, Sublime Text configurations
3. **Temporary Files:** Ignores backups, swap files, and logs
4. **Future-Proof:** Includes entries for Node.js dependencies and build tools (in case you add them later)
5. **Security:** Prevents accidentally committing environment variables

**How to Use:**

1. Save this as `.gitignore` in your repository root (same directory as `index.html`)
2. If you've already committed any of these files, remove them from Git:
```bash
   git rm --cached .DS_Store
   git commit -m "Remove .DS_Store from tracking"
   
**Pro Tip for macOS Users:**

To prevent `.DS_Store` creation on network volumes globally:
```bash
defaults write com.apple.desktopservices DSDontWriteNetworkStores true

This `.gitignore` will keep your repository clean and prevent macOS-specific junk from cluttering your GitHub Pages site! 🎯